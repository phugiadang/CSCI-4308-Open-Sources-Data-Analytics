#Programs:

##1. threadCass.py
###This progam streams tweets about presidential candidates, and stores the tweets into our Cassandra database.
##
##
##2. query.py
###This program allows the user to query our Cassandra database for tweet counts in a given time range. The user can specify the candidate and date range.
